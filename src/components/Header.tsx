const Header = (): JSX.Element => (
  <>
    <h1 className="text-4xl font-thin">
      Weather <span className="font-black">Forecast</span>
    </h1>
    <p className="text-sm mt-2">Enter a city below.</p>
  </>
);

export default Header;
